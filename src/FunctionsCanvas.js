var canvas = document.getElementById("Canvas");
     function fullScreen() {
        canvas.style.width = window.innerWidth + "px";
        canvas.style.height = window.innerHeight + "px";
     };
     fullScreen();

     //probar con crear el canvas